# Optional. Leave unset to auto-detect: the app calls the API on whatever
# host was used to load the page, on port 8000. Only set this if the API
# lives on a different host/port than the frontend.
# VITE_API_BASE_URL=http://localhost:8000
